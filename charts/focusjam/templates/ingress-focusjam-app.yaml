{{- if .Values.focusjamApp.ingress.enabled -}}
{{- $serviceName := include "focusjam.fullname" . -}}
{{- $servicePort := .Values.focusjamApp.service.externalPort -}}
apiVersion: {{ include "focusjam.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ include "focusjam.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "focusjam.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart:  {{ include "focusjam.chart" . }}
  annotations:
    {{- if .Values.focusjamApp.ingress.annotations }}
{{ toYaml .Values.focusjamApp.ingress.annotations | indent 4 }}
    {{- end }}
    {{- if .Values.focusjamApp.ingress.tls }}
    kubernetes.io/tls-acme: 'true'
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    {{- else }}
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    {{- end }}
spec:
  {{- if .Values.focusjamApp.ingress.ingressClassName }}
  ingressClassName: {{ .Values.focusjamApp.ingress.ingressClassName }}
  {{- end }}
  rules:
  {{- range $host := .Values.focusjamApp.ingress.hosts }}
  - host: {{ $host }}
    http:
      paths:
      - path: /
        {{- if $.Capabilities.APIVersions.Has "networking.k8s.io/v1" }}
        backend:
          service:
            name: {{ $serviceName }}
            port:
              number: {{ $servicePort }}
        pathType: Prefix
        {{- else }}
        backend:
          serviceName: {{ $serviceName }}
          servicePort: {{ $servicePort }}
        {{- end }}
  {{- end -}}
  {{- if .Values.focusjamApp.ingress.tls }}
  tls:
{{ toYaml .Values.focusjamApp.ingress.tls | indent 4 }}
  {{- end -}}
{{- end -}}
