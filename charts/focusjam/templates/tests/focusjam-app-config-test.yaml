apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include  "focusjam.fullname" . }}-app-tests
  labels:
    app.kubernetes.io/name: {{ include "focusjam.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "focusjam.chart" . }}
data:
  run.sh: |-
    #!/usr/bin/env bats
    @test "Testing FocusJam is accessible" {
      url="http://{{ include "focusjam.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.focusjamApp.service.internalPort }}"
      code=$(wget --server-response --spider --timeout 10 --tries 1 ${url} 2>&1 | awk '/^  HTTP/{print $2}')
      [ "$code" == "200" ]
    }
