{{- if not .Values.global.existingLicenseSecret.name }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "focustalk.fullname" . }}-mattermost-license
  labels:
    app.kubernetes.io/name: {{ include "focustalk.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart:  {{ include "focustalk.chart" . }}
type: Opaque
data:
  focustalk.mattermost-license: {{ .Values.global.mattermostLicense | b64enc | quote  }}
{{- end -}}