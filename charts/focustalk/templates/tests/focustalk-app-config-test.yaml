apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include  "focustalk.fullname" . }}-app-tests
  labels:
    app.kubernetes.io/name: {{ include "focustalk.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "focustalk.chart" . }}
data:
  run.sh: |-
    #!/usr/bin/env bats
    @test "Testing Focus Talk is accessible" {
      url="http://{{ include "focustalk.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.focustalkApp.service.internalPort }}"
      code=$(wget --server-response --spider --timeout 10 --tries 1 ${url} 2>&1 | awk '/^  HTTP/{print $2}')
      [ "$code" == "200" ]
    }