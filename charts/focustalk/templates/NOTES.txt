Focus Talk has been deployed!

IMPORTANT: This is a minimal deployment requiring external database and file storage.
Make sure you have configured:
  - External database connection (PostgreSQL)
  - File storage via environment variables (S3, GCS, etc.)

Get the application URL by running these commands:
{{- if .Values.focustalkApp.ingress.enabled }}
  Ingress is enabled. Access Focus Talk at:
  {{- range $host := .Values.focustalkApp.ingress.hosts }}
    http{{ if $.Values.focustalkApp.ingress.tls }}s{{ end }}://{{ $host }}
  {{- end}}
{{- else if contains "NodePort" .Values.focustalkApp.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "focustalk.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.focustalkApp.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "focustalk.fullname" . }} -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
  echo http://$SERVICE_IP:{{ .Values.focustalkApp.service.externalPort }}
{{- else if contains "ClusterIP"  .Values.focustalkApp.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "focustalk.name" . }}" -o jsonpath="{.items[0].metadata.name}")
  echo "Visit http://127.0.0.1:8065 to use your application"
  kubectl port-forward $POD_NAME 8065:{{ .Values.focustalkApp.service.externalPort }}
{{- end }}

To check the status of your deployment:
  kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "focustalk.name" . }}"
